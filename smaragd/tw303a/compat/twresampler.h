#ifndef TW_COMPAT_TWRESAMPLER_H
#define TW_COMPAT_TWRESAMPLER_H
// Forwarding header (proposal 14, Phase 2). The real header moved to the
// module below; new code should include it by that path. This file exists so
// pre-modularization include strings keep compiling and will be removed once
// the app is migrated.
#include "tw/sources/twresampler.h"
#endif
