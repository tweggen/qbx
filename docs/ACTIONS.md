# ACTIONS — the command surface (generated reference)

Every user-visible mutation goes through an `SAction` (see
`smaragd/main/actions/CONTRACT.md`). This maps each registered verb to its
class, source location, and XML attributes, for writing `.qxa` scripts and
headless tests. Keep it truthful when adding or changing an action.

Conventions (see also `smaragd/main/testkit/CONTRACT.md`):
- positions/durations are FRAMES at the project rate; fraction strings like
  `"48000/1"` parse via `parseFractionOrDouble`.
- clip/track paths are comma-separated child indices from the root mixer,
  e.g. `clip="0,1"` = track 0, child 1.
- a rejected `apply()` FAILS a headless test unless the action element has
  `expectReject="true"`.

| Verb | Class | Source (under smaragd/main/) | Attributes (name = default) |
|---|---|---|---|
| `add-automation-lane` | SAddAutomationLaneAction | objects/track/src/sautomationactions.cpp | `owner` (index path from the root mixer; the TARGET's space decides how it is resolved — `self:` as a LANE, `param:` as a lane plus `slotIndex`, `cut:` as a PLACEMENT), `target` (`self:Volume` \| `self:Muted` \| `param:<id>` \| `cut:Gain` \| `cut:VelocityScale` \| `cut:Transpose`), `slotIndex` = "-1", `take` = "-1" (< 0 = the ACTIVE take of a stack), `mode` = "trim" (off\|trim\|read\|touch\|latch\|write), `name` = "" (recovery name for a `param:` lane; resolution is always by id), child `<p t= v= c= k=/>` elements. ABSOLUTE: on a lane that already exists it writes the mode and the whole point list, which is what makes it the inverse of `remove-automation-lane`. Lanes are OWNER-HELD and never `SLink` children — see design §3.3 |
| `add-automation-point` | SAddAutomationPointAction | objects/track/src/sautomationactions.cpp | `owner` (index path from the root mixer; the TARGET's space decides how it is resolved — `self:` as a LANE, `param:` as a lane plus `slotIndex`, `cut:` as a PLACEMENT), `target` (`self:Volume` \| `self:Muted` \| `param:<id>` \| `cut:Gain` \| `cut:VelocityScale` \| `cut:Transpose`), `slotIndex` = "-1", `take` = "-1" (< 0 = the ACTIVE take of a stack), `time` (FRAMES in the owner's domain — timeline for a track or a slot, CLIP-RELATIVE for a `cut:` lane), `value` (the target's OWN units: dB for `self:Volume`, 0/1 for `self:Muted`, the plugin's host-facing domain for `param:` — normalized for VST3 — and a LINEAR factor for `cut:Gain`), `curve` = "linear" (step\|linear\|exp), `tension` = "0" (exp only). A point already on that frame is REPLACED; the lane is created if absent |
| `add-event` | SAddEventAction | objects/midi/src/smidieventactions.cpp | `clip`, `kind` = "cc" (a kind NAME, or `0xNN` for a raw SMF meta type), `tick` (the sequence's own musical ticks, PPQ 960), `channel` = "-1", `key` = "-1", `p` = "0" (CC number / meta type / sysex status byte), `v` = "0", `v2` = "0", `text` = "", `blob` = "" (hex), `take` = "-1". One non-note event. The inverse is a `set-events` carrying the previous table, which is what every event verb's inverse is |
| `add-midi-take` | SAddMidiTakeAction | objects/midi/src/smidirecordactions.cpp | `clip`, `index` = "-1" (append), `activate` = "1", `name` = "", `lengthTicks` = "0" (0 = derive the sequence's length from the column's duration through the tempo map), plus `<e .../>` / `<n .../>` CHILDREN carrying the take's events in the target sequence's own TICKS. `add-take` for EVENTS, and a separate verb because `add-take` is audio-only by construction (it addresses a FILE and seeds grain params). `clip` may be a plain `SMidiCut` placement - wrapped into a column first, becoming take 0 - or an existing column. A column of AUDIO takes REFUSES it (homogeneity, 37 D8b) and the action is rejected rather than building a column that plays notes or audio depending on which lane is active. It reaches the column through the generic seam on `SObject` and never names `STakeStack`, because `objects/midi` sits at the RANK of `objects/cut`. Inverse: `remove-midi-take`, which captures the take's events so ITS inverse restores them |
| `add-note` | SAddNoteAction | objects/midi/src/smidieventactions.cpp | `clip`, `tick` (musical TICKS at PPQ 960 - the content is tick-native, D2), `dur` = "480", `key` = "60", `velocity` = "100", `channel` = "0", `releaseVelocity` = "64", `take` = "-1", `broadcast` = "1" (edit groups). Notes are stored WITH their length; there is no separate note-off to add |
| `add-sample` | SAddSampleAction | objects/cut/src/saddsampleaction.cpp | `trackPath` = "0" (index-path from the root mixer, so a lane nested in a folder track is addressable; the legacy top-level-only `trackIndex` is still accepted on read), `filePath` = "", `timePos` = "0" |
| `add-take` | SAddTakeAction | objects/cut/src/saddtakeaction.cpp | `clip`, `filePath`, `startOffset` = "0", `index` = "-1", `activate` = "1", `stretch` = "1.0", `pitchCents` = "0" |
| `add-to-selection` | SAddToSelectionAction | selection/src/saddtoselectionaction.cpp | `paths` = "" |
| `add-track` | SAddTrackAction | objects/mixer/src/saddtrackaction.cpp | `index` = "-1" |
| `arm-track` | SArmTrackAction | objects/track/src/sliveinputactions.cpp | `trackPath`, `armed` = "0" (ABSOLUTE). Record-arm, which is also MONITOR-arm (design D9: one flag). It used to be a direct model write on the grounds that arming is transport state rather than project state; it is a verb since proposal 21 L1b because it decides whether a track is MONITORED, which re-wires the mixer and hands a plugin chain to another thread - so it has to pass through the one place that owns that ordering (`SAppContext::liveLanesChanged`) - and because a user who armed the wrong lane of a multi-selection has the same right to undo it as one who muted it. `ArmedForRecording` still round-trips through the project file and is INERT on load: a loaded project never starts monitoring |
| `assert-audio-continuity` | SAssertAudioContinuityAction | testkit/src/slivetestactions.cpp | `filename`, `channel` = "0", `startFrame` = "0", `frameCount` = "-1", `maxGapFrames` = "-1" (< 0 = not checked), `maxStep` = "-1" (< 0 = not checked), `silenceLevel` = "0.001". TWO independent measurements over one pass, because a hand-over can fail in two ways and neither subsumes the other: `maxGapFrames` bounds the longest RUN of frames below `silenceLevel` (a live lane handing back to the frozen sum drops out for exactly that many frames when the epoch gate is wrong), `maxStep` bounds the largest |x[n] - x[n-1]| (a source summed twice, or a block served twice, shows up here and nowhere else). Proposal 21 L1b |
| `assert-audio-energy` | SAssertAudioEnergyAction | testkit/src/sassertaudioenergyaction.cpp | `filename` = "", `minRms` = "0.01", `maxRms` = "0.95", `startFrame` = "0", `frameCount` = "-1" (< 0 = to the end of the file), `channel` = "-1" (-1 = all channels pooled; a channel the file does not have is REJECTED, not silently empty). `channel` is honoured whether or not `frameCount` is given — it used to be dropped on the whole-file path (proposal 36 M0). `filename` resolves in the test output dir, else the .qxa's directory, else the cwd, so a committed fixture (`../test_channels4.wav`) is addressable |
| `assert-audio-frequency` | SAssertAudioFrequencyAction | testkit/src/sassertaudiofrequencyaction.cpp | `filename` = "", `minHz` = "0", `maxHz` = "0", `startFrame` = "0", `frameCount` = "-1", `channel` = "-1" (autocorrelation f0 — the pitch gate) |
| `assert-audio-length` | SAssertAudioLengthAction | testkit/src/sassertaudiolengthaction.cpp | `filename` = "", `minFrames` = "0" (no lower bound), `maxFrames` = "-1" (no upper bound) — asserts HOW LONG a rendered file is, which is the behaviour a whole-project render's extent decides. Reads the file HEADER, not the samples, because a zero-frame file (the render of an empty arrangement) is a legal answer and the RMS analysers reject an empty region |
| `assert-audio-onset` | SAssertAudioOnsetAction | testkit/src/slivetestactions.cpp | `filename`, `channel` = "0", `startFrame` = "0", `threshold` = "0.02", `window` = "64", `minFrame` = "-1", `maxFrame` = "-1" (< 0 = unbounded), `afterMidiIn` = "0". WHERE a sound starts, in frames, and it REPORTS the number whether the bounds pass or fail — "the live instrument sounded 4544 frames after the key" is the measurement, not a boolean. The scan is a running RMS window, not a per-sample threshold: one sample above the line is a click or a dither bit. `afterMidiIn="1"` measures the onset RELATIVE to the last `midi-in-event` / `midi-in-replay` injection, mapping its host time to a capture frame through `CaptureBackend::frameAtHostTime` — the AUDIO backend's own block log, which shares no code with the live lane — so the bounds then apply to the DIFFERENCE. Needs the capture audio backend and `SMARAGD_CAPTURE_SPEED=1`. Proposal 21 L2 |
| `assert-audio-peak` | SAssertAudioPeakAction | testkit/src/sassertaudiopeakaction.cpp | `filename` = "", `maxPeak` = "0.95", `startFrame` = "0", `frameCount` = "-1" (< 0 = to the end of the file), `channel` = "-1" — same channel and path rules as `assert-audio-energy`. The pooled peak is the MAX over channels, so a dropped `channel=` could only ever mis-report a QUIET channel as loud |
| `assert-automation-value` | SAssertAutomationValueAction | testkit/src/sassertautomationvalueaction.cpp | `owner` (index path from the root mixer; the TARGET's space decides how it is resolved — `self:` as a LANE, `param:` as a lane plus `slotIndex`, `cut:` as a PLACEMENT), `target` (`self:Volume` \| `self:Muted` \| `param:<id>` \| `cut:Gain` \| `cut:VelocityScale` \| `cut:Transpose`), `slotIndex` = "-1", `take` = "-1" (< 0 = the ACTIVE take of a stack), `time`, `value`, `tolerance` = "1e-6", `mode` = "" (optional: the lane's mode must be this), `pointCount` = "-1" (optional). Asks the SNAPSHOT the engine is handed (`SAutomationLane::valueAt`), not a re-implementation of the interpolation, so a case can pin the midpoint of a ramp as a closed form without rendering it. A MISSING lane is REJECTED, never reported as the default value — pair with `expectReject="true"` to assert a lane is absent |
| `assert-channels-differ` | SAssertChannelsDifferAction | testkit/src/sassertchannelsdifferaction.cpp | `filename` = "", `channelA` = "0", `channelB` = "1", `minRmsDelta` = "0.01" (minimum \|rms(A) - rms(B)\|, a LEVEL discriminator), `minDiffRms` = "-1" (minimum rms(A - B), a CONTENT discriminator; < 0 = not checked), `startFrame` = "0", `frameCount` = "-1" — "these two channels are genuinely different audio", assertable rather than inferred (proposal 36 M0). Same channel twice, or a channel the file lacks, is REJECTED. **Proposal 36 B5: this now PASSES on a real render** — the sink carries the project's channels, so `mc_stereo_clip_width`, `mc_six_channel`, `mc_playback_channels` and `plugin_stereo_chain` assert the positive direction. `expectReject="true"` still has work to do, but it now pins CONTENT rather than the sink: a §4.4 clamp (channels 2..7 of a 2-channel source are its last channel), or a fixture whose own channels are equal (`test_sawtooth.wav`, §7 trap 22) |
| `assert-clip-channels` | SAssertClipChannelsAction | testkit/src/sassertclipchannelsaction.cpp | `clip` = "" (index-path to the clip's SLink, same spelling `slip-clip` uses), `position` = "0" (CLIP-RELATIVE frames), `expectChannels` = "0" (0 = not checked), `channelA` = "0", `channelB` = "1", `minRmsDelta` = "0.01" (LEVEL discriminator), `minDiffRms` = "-1" (CONTENT discriminator; < 0 = not checked) — resolves the clip through `SCut::resolveClip` (the same one-snapshot fold `twView::freezePage` uses), declares a demand on the project's `CaptureRevalidator` and waits, then reads the page the RESOLVED component published. Proposal 36 AC B3.2: written when a clip's channel width was not observable in any rendered file at all. B5 widened the sink, so a FILE can show channels now — but only after the track mix, the chain, the rewire and the master sum; this verb is still the only place a CLIP's own width is observable with nothing downstream in the way. It is the SCHEDULER path, unlike `assert-meter`, which drives the legacy pull. An out-of-range channel is REJECTED, never a silent compare of channel 0 with itself. Pair with `expectReject="true"` to pin a clip as still MONO |
| `assert-clip-window` | SAssertClipWindowAction | testkit/src/sassertclipwindowaction.cpp | `clip`, `startTime`/`duration`/`loopLength`/`startOffset` = "-1" (not checked; TIMELINE FRAMES), `timebase` = "" (beats\|time), `take` = "-1". Reads the `SClipWindow` interface, so it works for an audio cut, an event clip and any later window type. It exists because `set-tempo` has to be gated on exactly this - a beats-timebase MIDI clip MOVES and an audio clip does not - and a rendered assertion cannot separate "the clip moved" from "the clip moved and its content moved back" |
| `assert-file-identical` | SAssertFileIdenticalAction | testkit/src/sassertfileidenticalaction.cpp | `actual` = "", `expected` = "", `maxReportedDiffs` = "8" — byte comparison, with both paths resolved like `filename` above. On mismatch it reports both sizes, the offset and byte values of the FIRST difference, how many bytes differ, and the first few offsets, so a truncated render and a re-rendered one do not read alike. Pair with `expectReject="true"` to assert two files DIFFER (see file_identical_gate.qxa), `startFrame` = "0", `frameCount` = "-1" (proposal 37 P0a: given a range both files are parsed as RIFF/WAVE, their formats must agree, and only those frames of the `data` chunk are compared; an ABSOLUTE path is used as given, so a golden from another process is nameable) |
| `assert-lane-alignment` | SAssertLaneAlignmentAction | testkit/src/slanelayouttestactions.cpp | `grabPng` = "", `grabWidth` = "0", `grabHeight` = "0" — fails unless every track head sits exactly on its lane (same top, same height as the painter uses, full column width) and the lane geometry is self-consistent (row tops are the running sum of the row heights and the y→row lookup inverts it at both edges of every lane). **Since proposal 37 P6** it also covers AUTOMATION sub-lanes: they are sub-lanes by the same rule take lanes are, so one head still spans the whole group, and every automation row must hang off its own track's lane group and name an owner the model can resolve. Assert it AFTER moving the view (`set-lane-view`) — that is when the two used to come apart. `grabPng` paints the arranger CANVAS into the test output directory (coverage, never an oracle) |
| `assert-log` | SAssertLogAction | testkit/src/sassertlogaction.cpp | `contains` (literal substring, not a regex), `minCount` = "1", `maxCount` = "-1" (no upper bound; `minCount="0" maxCount="0"` asserts a line is ABSENT), `level` = "" (any, or error\|warn\|info\|debug\|trace) — reads the in-process `TwLog` ring, which every channel funnels into (`TW_LOG*`, `syslog()`, and Qt's qWarning/qDebug through the installed handler). There is NO log file under `--test-case` (main.cpp skips the file sink so a headless suite cannot race over one file), and the ring's capacity is raised for such a run so a long render cannot evict the line under test. THE WINDOW is the records logged since the PREVIOUS action started; two assert-logs in a row share one window, so both examine the action they follow. This is the only way to gate a RECOVERY — the audio of a repaired project is exactly the audio of one that never needed repairing |
| `assert-meter` | SAssertMeterAction | testkit/src/smetertestactions.cpp | `trackPath` = "" (index-path from the root mixer — needed to meter a NESTED lane; falls back to `trackIndex`), `trackIndex` = "0", `position` = "0" (project frames), `minRms`/`maxRms`/`minPeak`/`maxPeak` = "-1" (< 0 = not checked), `expectMiss` = "false", `headHeight` = "0", `headWidth` = "0" (0 = the minimal SMV_TRACK_CTRL_WIDTH column), `contains` = "", `grabPng` = "", `grabVertical` = "true", `grabHead` = "" — freezes the page covering `position` itself (no transport needed) and runs the production `twLevelProbe`; with `headHeight` > 0 it also builds the REAL track head off screen via `SMainWindow::describeTrackMeter` and matches `SLevelMeter::describe()`, which reads `vis=…\|orient=…\|lanes=…\|width=…\|len=…\|peak=…\|rms=…\|hold=…\|clip=…\|db=…` (the per-lane fields are comma-separated in lane order, so a one-lane meter reads exactly as it did before proposal 36 B8). `grabPng` paints the measured level into a PNG (the only coverage of `SLevelMeter::paintEvent`); `grabHead` paints the REAL head at `headHeight` x `headWidth` with that level in its bars — AC B8.4's evidence, and the reason the head is pushed a level directly: a head built for a grab is never SHOWN, so `onMeterTick`'s isVisible() gate would leave every bar at the floor. **Lanes (proposal 36 B8):** `lanes` = "1" (how many to ask the probe for; it answers `min(lanes, page->channels())` per §4.4), `expectLanes` = "-1" (>= 0 pins the lane count — assert the WIDTH before believing any level, or a page that quietly stayed mono passes every bound on lane 0), `lane` = "0" (which lane the min/max bounds apply to; out of range is an ERROR, never a silent rms 0), `laneA`/`laneB` = "-1" and `minLaneRmsDelta` = "-1" (the CHANNEL claim: |rms(A) − rms(B)| must reach it). A missing `lanes` is inferred from the highest lane index mentioned. Never make a lane claim over `test_sawtooth.wav`, whose two channels are byte-identical (proposal 36 trap 22) — `meter_levels.qxa` uses it as the negative control instead. Measures on the LEGACY PULL path, so set a track's gain BEFORE first probing a position (see the caveat in smetertestactions.cpp) |
| `assert-master-sums` | SAssertMasterSumsAction | testkit/src/sasserttrackchannelsaction.cpp | `position` = "0" (absolute timeline frames; the containing page is used), `tolerance` = "0.0005" (maximum absolute per-sample deviation — float32 summation is not associative, so an exact comparison would be wrong rather than strict), `stride` = "97" (compare every Nth frame; coprime with the page size so the sampled set is not a periodic pattern), `minRms` = "0" (lower bound on the master's channel 0, so a project that rendered silence cannot pass the sum trivially) — freezes the root mixer's component and every AUDIBLE top-level track's root component at the same page and checks master[c][i] == sum over tracks of track[c][i] for EVERY channel. Proposal 36 AC B4.1. Muted and soloed-out lanes are excluded because the mixer excludes them by nulling their input plug. A narrower track contributes its LAST channel to every channel above it (§4.4's clamp), exactly as `twMixer` does. The pre-B4 master ran `bus < 1` and dropped every track's second channel, which this fails |
| `assert-midi-events` | SAssertMidiEventsAction | testkit/src/smiditestactions.cpp | `scope` = "clip" (the cut's own frame-domain snapshot) or "feed" (`STrack::eventFeed()->collect()`, the merge an instrument will read - the ONLY way to see mute / solo / midiRouting), `clip` (scope=clip), `trackPath` (scope=feed), `take` = "-1", `kind` = "" (note\|noteon\|noteoff\|**noteoff-synth**\|noteoff-real\|cc\|bend\|program\|sysex\|tempo\|timesig\|meta\|any), `at` = "-1" (FRAMES; clip scope is clip-relative), `tolerance` = "0", `key` = "-1", `velocity` = "-1", `velocityTolerance` = "0.5", `channel` = "-1", `count`/`minCount`/`maxCount` = "-1" (not checked), `startFrame` = "0", `frameCount` = "-1", `contains` = "" (substring of the dumped event list). A `noteoff*` kind runs a real `collect` over the clip's window PLUS ONE FRAME, because a note-off SYNTHESISED at the clip end lands on the half-open boundary and belongs to the window that starts there (events/CONTRACT inv. 8-9) - it exists in no table, so nothing else can see it |
| `assert-midi-file` | SAssertMidiFileAction | testkit/src/smiditestactions.cpp | `filename` (resolved like `assert-audio-energy`'s), `trackCount`/`noteCount`/`eventCount`/`firstTick`/`ppq`/`format` = "-1" (not checked). The counts-and-shape companion of `assert-file-identical`: byte identity is the gate for a file twSmf AUTHORED, this is what a foreign file (or an export whose bytes may legitimately differ) can be held to |
| `assert-midi-options` | SAssertMidiOptionsAction | testkit/src/smidiouttestactions.cpp | `contains` = "", `absent` = "", `outputPorts`/`inputPorts` = "-1" (not checked). Builds the REAL `SOptionsDialog` OFF SCREEN and matches against `SOptionsDialog::describeMidiPage()` - the house pattern for a page made of widgets, and the only coverage of the Options -> MIDI page's load path. The port lists come from the ACTIVE MIDI backend, so a headless run (capture) lists exactly one endpoint on each side |
| `assert-monitor-latency` | SAssertMonitorLatencyAction | testkit/src/slivetestactions.cpp | `filename` (the capture dump), `inputFile` (the WAV that was played into the input), `channel` = "0", `startFrame` = "24000", `frameCount` = "24000", `maxFrames` = "8192", `searchFrames` = "24000", `minCorrelation` = "0.5". How far behind the input the MONITORED output is, by NORMALISED cross-correlation - deliberately not by the position decoder, which resolves 4096-frame blocks (design F11) and could not tell a pass from a fail at this scale. The peak is a similarity in [-1, 1], so the threshold means the same thing whatever the monitored level is; the input index WRAPS, because `FileAudioInput` loops. Reports the lag in frames and in ms. Proposal 21 L1b |
| `assert-midi-recorded` | SAssertMidiRecordedAction | testkit/src/smidirecordtestactions.cpp | `trackPath`, `clips` = -1, `takes` = -1, `minTakes` = -1, `passes` = -1, `minPasses` = -1, `notes` = -1, `minNotes` = -1, `events` = -1 (non-note events), `startFrame` = -1, `startTolerance` = "1024", `durationFrames` = -1, `durationTolerance` = "4096", `mode` = "", `quantize` = "", `contains` = "" (substring of `SMidiRecorder::describe()`). The MIDI sibling of `assert-recorded-clip`, and it deliberately does NOT assert where a note landed - that is `assert-midi-events`' job over the clip's own snapshot, which is a far better instrument for it. What this checks is the SHAPE of what was committed, from BOTH sides at once: the model (how many event columns, how many takes) and the recorder's own report (how many passes, how many notes). It also asserts ONE TAKE PER PASS unconditionally whenever more than one pass was committed - that half of the loop-recording claim is not wall-clock-dependent, whereas the pass COUNT is, hence `minTakes`/`minPasses` (proposal 21 L4) |
| `assert-recorded-clip` | SAssertRecordedClipAction | testkit/src/srecordtestactions.cpp | `trackPath`, `clips` = -1, `takes` = -1, `minTakes` = -1, `minPasses` = -1, `passes` = -1, `startFrame` = -1, `startTolerance` = "1024", `durationFrames` = -1, `durationTolerance` = "4096", `minDurationFrames` = -1, `inputLatencyFrames` = -1, `outputLatencyFrames` = -1, `userOffsetFrames`, `compensationFrames`, `trimmedFrames` = -1, `growing` = "false", `previewNonEmpty` = "false", `sourceAtStartFrame` = -1, `sourceTolerance` = "4096". Every numeric expectation is opt-in (the sentinel means unchecked), so one verb serves the mid-take assertions and the post-take ones. It ALSO checks the placement IDENTITY on every call once the take is over — the clip's link start must equal `placementFrame(trimmed)` — so a recorder that reported the right terms and applied different ones fails even when no expectation was given. It also asserts ONE TAKE PER PASS unconditionally whenever more than one pass was committed — that half of the loop-recording claim is not wall-clock-dependent, whereas the pass COUNT is, which is why `minTakes`/`minPasses` exist (proposal 21 L3b) |
| `assert-render-policy` | SAssertRenderPolicyAction | testkit/src/slivetestactions.cpp | `liveThreadRefusals` = "0", `liveOwnedRefusals` = "0", both MAXIMA. The first counts freezes reached from the live PUMP thread (which is `RenderPolicy::Never`), the second freeze-path renders that arrived at a live-owned processor and were answered with silence. A non-zero bound is legal but must be spelled out in the case that wants it and justified there - never defaulted. Proposal 21 L0/L1b |

| `assert-render-policy` | SAssertRenderPolicyAction | testkit/src/slivetestactions.cpp | `liveThreadRefusals` = "0", `liveOwnedRefusals` = "0", both MAXIMA. The first counts freezes reached from the live PUMP thread (which is `RenderPolicy::Never`), the second freeze-path renders that arrived at a live-owned processor and were answered with silence. A non-zero bound is legal but must be spelled out in the case that wants it and justified there - never defaulted. `minLiveOwnedRefusals` = "0" is the MINIMUM, for the one case that asserts the guard FIRED (`live_instrument_ownership`, proposal 21 L2 AC4): "at most 0" and "at least 1" are different claims and a case that wants the second has to say so. Proposal 21 L0/L1b/L2 |
| `assert-input-meter` | SAssertInputMeterAction | testkit/src/slivetestactions.cpp | `trackPath`, `minPeak` = "-1", `maxPeak` = "-1" (< 0 = not checked), `contains` = "" (a substring of `SLiveMonitor::describe()`, e.g. `live=on`). The PRE-FX input meter of an armed track: the peak the live source recorded of what it pulled out of the device ring. Deliberately not a `twLevelProbe` read - there is no frozen page for a live input, and the point of the input meter is to show signal BEFORE the chain touches it. Proposal 21 L1b |
| `assert-midi-out` | SAssertMidiOutAction | testkit/src/smidiouttestactions.cpp | `port` = "", `kind` = "" (noteon\|noteoff\|cc\|pitchbend\|programchange\|channelpressure\|polypressure\|any), `channel` = "-1" (0-BASED, like `add-note`'s), `key` = "-1", `cc` = "-1", `value` = "-1" (the message's LAST data byte), `at` (SIGNED project frames since playback started; omitted = position not checked), `tolerance` = "4096", `count`/`minCount`/`maxCount` = "-1", `index` = "-1" (the first match must be message number N in the recording - the only way to assert ORDER across kinds). THE MEASUREMENT IS INDEPENDENT OF THE PUMP (design D6, review #12): the capture MIDI port records `{hostTimeNs, port, bytes}` and deliberately NOT the due time it was asked for, and `at` is `CaptureBackend::frameAtHostTime(sent) - deviceOutputLatencyFrames` - the project frame whose AUDIO was being heard when the message left, taken from the audio pump's own block log. REJECTS unless `SMARAGD_MIDI_BACKEND` is `capture` (a headless run defaults to it) and the audio backend is the capture backend. Requires `SMARAGD_CAPTURE_SPEED=1`. `maxLagMs` = "-1" (< 0 = not checked) bounds MIDI-THRU (proposal 21 L2 AC5): how long after the last `midi-in-event` injection the matched messages reached the wire, in MILLISECONDS, HOST TIME TO HOST TIME with no frame mapping anywhere in it — thru has no position, it is a key being pressed right now. The measured worst lag is printed on success as well as on failure |
| `assert-track-channels` | SAssertTrackChannelsAction | testkit/src/sasserttrackchannelsaction.cpp | `trackPath` = "" (index-path from the root mixer, so a NESTED lane is reachable), `position` = "0" (absolute timeline frames), `expectChannels` = "0" (0 = not checked), `channelA` = "0", `channelB` = "1", `minRmsDelta` = "0.01" (LEVEL discriminator), `minDiffRms` = "-1" (CONTENT discriminator; < 0 = not checked), `minRms` = "-1" (lower bound on channelA, so a page of the right width full of SILENCE cannot satisfy the others trivially), `driver` = "scheduler" or "pull" — reads the page published by `STrack::getRootComponent()`, the same `twRewire` the level meters read and the master sums. Proposal 36 AC B4.7, the assertion B3 could not make because the track path was still one channel wide there. `driver="pull"` takes the LEGACY recursive freeze instead of a scheduler demand: it is the path `assert-meter` drives, the only one available under `SMARAGD_REVAL_WORKERS=0`, and AC B4.6's subject. An out-of-range channel is REJECTED, never a silent compare of channel 0 with itself; pair with `expectReject="true"` to pin a track as still mono |
| `assert-plugin-strip` | SAssertPluginStripAction | testkit/src/spluginuitestactions.cpp | `trackPath` = "" (index-path from the root mixer — the only way to reach a track NESTED in a folder; falls back to `trackIndex` when absent), `trackIndex` = "0" (legacy top-level), `slotCount` = "-1" (rows the FX strip rendered), `slotIndex` = "-1", `contains` = "", `absent` = "" — builds the REAL `SPluginEffectStrip` off screen and matches `describeSlot(slotIndex)`, which reads `name=…|state=…|mode=…|bypass=…|nameEnabled=…|bypassEnabled=…|editEnabled=…|reload=…|trackPath=…|tooltip=…`. `trackPath=` is the strip's RESOLVED track path: it is the strip's single point of failure (every button handler early-returns when it is empty, so a wrong answer disables the whole strip silently) and is otherwise invisible from outside. Since proposal 37 P3b the description also carries `kind=instrument\|effect`, DERIVED from the descriptor, between `mode=` and `bypass=` — deliberately not next to `name=`, because the M5 cases assert `name=…\|state=…` as one contiguous span |
| `assert-instrument-slot` | SAssertInstrumentSlotAction | testkit/src/spluginuitestactions.cpp | `trackPath` = "" (index-path; falls back to `trackIndex` = "0"), `present` = "1" (0 asserts there is NO instrument), `uid` = "", `format` = "", `mode` = "" (the processor's row: `DirectGen` / `MonoSpread` / `GenFold` / `WideGen`, or `Transparent` for a layout with no defined spread), `state` = "" (`Active` / `Missing` / `Unsupported`), `minTailFrames` = "-1", `maxTailFrames` = "-1", `hasFeed` = "-1" (whether the processor holds an event source — the one thing that decides whether notes reach it). Asks `STrack::instrumentSlot()` the same question the engine, the head glyph and the FX strip ask; builds no widget and runs no render |
| `assert-event-editor` | SAssertEventEditorAction | testkit/src/seventuitestactions.cpp | `clip` = "" (empty = follow the SELECTION, which is the production path), `kind` = "pianoroll" (the kind-registry key), `contains` = "", `absent` = "", `grabPng` = "" (bare filename, written into the test output dir), `grabWidth` = "720", `grabHeight` = "360" — binds the REAL `SEventEditorDock` (the fifth dock, built in SMainWindow's ctor and never shown) and matches `SEventEditorDock::describe()`, which reads `kind=…\|notes=N\|grid=…\|linked=0/1\|empty=0/1\|selection=N` plus the view's own fields (`tool=…\|topKey=…\|keyH=…\|ccLanes=…` for the piano roll). `contains` matches a CONTIGUOUS substring, so the field order is part of the contract. The PNG is coverage of the paint paths, never an oracle |
| `assert-track-head` | SAssertTrackHeadAction | testkit/src/seventuitestactions.cpp | `trackPath` = "0" (index-path, so a NESTED lane can be described), `headHeight` = "160", `contains` = "", `absent` = "" — builds the REAL track head off screen at that lane height (`SMainWindow::describeTrackHead`) and matches `SSMVMixerControl::describeHead()`: `density=Full\|Compact\|Tiny`, `w=…\|h=…\|btns=M,S,R,T,G,A\|I=0/1\|A=0/1\|fitW=…\|fitH=…\|name=beside\|inrow\|ownline`. `I` is shown only when slot 0 is an INSTRUMENT and `A` only in Full density while the button column still fits; `fitW`/`fitH` are the "hiding beats clipping" rule made assertable. **Since proposal 37 P6** the string ends `\|Amode=<off\|trim\|read\|touch\|latch\|write>` — the TRACK's automation mode, reported at EVERY density because the "A" button hides on a short lane while the mode does not stop existing; and `grabPng` = "" / `grabWidth` = "0" / `grabHeight` = "0" paint that same off-screen head into the test output directory (coverage of the button's per-mode colour, never an oracle) |
| `assert-track-selection` | SAssertTrackSelectionAction | testkit/src/strackselectionactions.cpp | `paths` = "" (SEMICOLON-separated index-paths — a path is itself comma-separated — compared as a SET, so click order is not part of the contract; `paths=""` asserts nothing is selected), `primary` = absent (when given, the path that must be the PRIMARY of the selection: the lane the Track Detail dock follows). A selected track that is no longer in the tree reports as `<detached>` rather than being silently dropped |
| `automation-write-tick` | SAutomationWriteTickAction | testkit/src/sautomationuitestactions.cpp | `owner` (index path from the root mixer; the TARGET's space decides how it is resolved), `target` (`self:Volume` \| `self:Muted` \| `param:<id>` \| `cut:Gain` \| …), `slotIndex` = "-1", `take` = "-1", `value`, `time` = the CURRENT LOCATOR (optional), `release` = "0". ONE live tick of a Touch/Latch/Write pass — the `slip-clip` shape: it feeds `SAutomationRecorder` and pushes NO undo step, exactly as a fader moving mid-pass does. The pass commits ONE `set-automation-points` when it ends (the transport stop, or `release="1"`), and that single action is the undo step. REJECTS when the lane does not exist or its mode is not touch/latch/write — a tick into the void would let a green case prove nothing |
| `clear-selection` | SClearSelectionAction | selection/src/sclearselectionaction.cpp | (none) |
| `create-asset` | SCreateAssetAction | objects/mixer/src/screateassetaction.cpp | `container`, `startOffset` = "0", `duration` = "0", `assetName` |
| `cycle-disable` | SCycleAction | actions/src/scycleaction.cpp | (none) |
| `cycle-enable` | SCycleAction | actions/src/scycleaction.cpp | (none) |
| `cycle-toggle` | SCycleAction | actions/src/scycleaction.cpp | (none) |
| `drag-automation-point` | SDragAutomationPointAction | testkit/src/sautomationuitestactions.cpp | `owner` (index path from the root mixer; the TARGET's space decides how it is resolved — `self:` as a LANE, `param:` as a lane plus `slotIndex`, `cut:` as a PLACEMENT), `target`, `slotIndex` = "-1", `take` = "-1", `time`, `value` (where the gesture PRESSES), `toTime`/`toValue` (where it releases; omitting both makes it a click), `modifiers` = "" ("ctrl"/"alt"/"shift"/"meta", "+"-joined). The `drag-clip-edge` twin: it works out where the addressed breakpoint IS on screen and sends REAL press/move/release events into the arranger canvas. Click on empty lane = ADD, drag from a point = MOVE, `modifiers="ctrl"` on a point = REMOVE, `modifiers="alt"` = bend that segment (tension), `modifiers="shift"` on empty space = marquee. Not undoable itself — the gesture submits its own verb, so `<undo count="1"/>` reverses it. The lane must be SHOWN (`set-lane-view showAutomation="1"`); a `cut:` target additionally needs `clipEnvelopes="1"`, and is REJECTED otherwise. The drop is pixel-quantised and then grid-snapped, so assert a grid position or a range |
| `drag-clip-edge` | SDragClipEdgeAction | testkit/src/sdragclipedgeaction.cpp | `track` = "0", `clip` = "0", `edge` = "end" / "start" / "body", `toTime`, `half` = "lower" (or "upper" = the loop half), `modifiers` = "" ("ctrl"/"alt"/"shift", "+"-joined) — drives the REAL mouse handlers, the only way to test clip gestures. `edge="body"` is required for slip/duplicate/move; drop is pixel-quantised, so assert on ranges |
| `drag-note` | SDragNoteAction | testkit/src/seventuitestactions.cpp | `clip` = "" (empty = the editor's current binding), `tick`, `key`, `channel` = "-1" (wildcard), `toTick` = "0", `toKey` = "-1" (defaults to `key` for a move), `edge` = "" (or "end" = resize), `lane` = "notes" (or "velocity", which needs `toValue` 1..127), `toValue` = "-1" — the `drag-clip-edge` twin: it works out where the addressed note is on screen and sends REAL press/move/release events to `SPianoRollView`, so the gesture arithmetic under test is the one a pointer drives. The drop is PIXEL-QUANTISED and then grid-snapped, so assert a grid position or a range, never an arbitrary tick. The gesture submits its own `set-notes` on release, so `<undo count="1"/>` reverses it |
| `drag-track` | SDragTrackAction | testkit/src/strackselectionactions.cpp | `trackPath` = "0" (the head that is grabbed), `targetRow` = "0" (lane row in the flattened tree), `mode` = "onto" (nest into that row's track) / "before" (insert at its top boundary; `targetRow` == the row count means below the last lane) — runs the REAL beginTrackDrag/updateTrackDrag/endTrackDrag. Grabbing a head that is part of the SELECTION drags the whole selection (outermost tracks only); grabbing any other head selects it first and drags it alone. Addressed by ROW, not pixels, so a script does not encode the current zoom. Not undoable itself: the drop pushes its own action, or a macro when several tracks moved |
| `dump-midi-capture` | SDumpMidiCaptureAction | testkit/src/smidiouttestactions.cpp | `filename` = "" (no path separators; written into the test output dir), `minCount` = "0". The MIDI twin of `dump-playback-capture` and used the same way - a DIAGNOSTIC, not an oracle: one line per captured message with its host time, the project frame it maps to through the audio clock, the port, the raw bytes and a decoded form. Same REJECT rule as `assert-midi-out` |
| `dump-playback-capture` | SDumpPlaybackCaptureAction | testkit/src/sdumpplaybackcaptureaction.cpp | `filename` = "" (no path separators; written into the test output dir), `minFrames` = "0" — writes what the CAPTURE backend recorded during playback as a 16-bit PCM WAV, so assert-audio-* / assert-source-position can be pointed at the PLAYBACK path instead of a render. Captured frame 0 is the first frame of the CURRENT playback session (the recording is cleared at each start), i.e. the locator position playback started from. REJECTS unless the active backend is the capture backend (a --test-case run selects it by default; otherwise SMARAGD_AUDIO_BACKEND=capture) |
| `duplicate-clip` | SDuplicateClipAction | objects/cut/src/sduplicateclipaction.cpp | `source`, `destTrack`, `startTime` = "0" |
| `export-midi-file` | SExportMidiFileAction | objects/midi/src/smidifileactions.cpp | `filePath` (used AS GIVEN, i.e. relative to the working directory - deliberately NOT resolved against the script directory, because nothing may write into the shared `tests/cases`), `clip` = "" or `trackPath` = "" (neither = the whole project, in arrangement order, one SMF track per event clip), `type` = "1". Not undoable. A file imported and exported straight back is BYTE-IDENTICAL, because twSmf has exactly one canonical spelling (events/CONTRACT inv. 15) - that is `midi_clip_roundtrip`'s gate |
| `grid-disable` | SGridAction | actions/src/sgridaction.cpp | (none) |
| `grid-enable` | SGridAction | actions/src/sgridaction.cpp | (none) |
| `grid-toggle` | SGridAction | actions/src/sgridaction.cpp | (none) |
| `group-track` | SGroupTrackAction | testkit/src/sgrouptrackaction.cpp | `trackPath` = "0", `ungroup` = "0" ("1" = Ungroup) — runs the arranger's REAL Group/Ungroup context-menu slots. They compose a MACRO of add-track / reparent-track / remove-track whose index arithmetic is the whole risk (a folder cannot be born in place for a nested lane), so re-spelling that macro in a script would test the script, not the gesture. Not undoable itself; the gesture pushes its own macro, so `<undo count="1"/>` reverses it |
| `import-midi-file` | SImportMidiFileAction | objects/midi/src/smidifileactions.cpp | `trackPath` = "", `filePath`, `timePos` = "0", `mode` = "tracks" (one clip per SMF track) \| "merged", `newTracks` = "1". One `SMidiSequence` per SMF track, materialised INLINE; the file's PPQ is rescaled to 960. ONE atomic composite - the tracks it creates are part of it, so undo removes them too. The first tempo meta becomes a `set-tempo` when the project is still EMPTY and otherwise only warns; every tempo event stays in the sequence either way, because dropping it would make an export write a different file than the import read |
| `insert-midi-clip` | SInsertMidiClipAction | objects/midi/src/smidiclipactions.cpp | `trackPath`, `timePos` = "0" (frames), `duration` = "0" (musical TICKS; 0 = one bar of the project's time signature), `name` = "". Sequence + window + placement in one step; the link is born `timebase=beats` (D2), so the clip stays on its bar across a tempo edit |
| `insert-plugin` | SInsertPluginAction | objects/track/src/sinsertpluginaction.cpp | `trackPath`, `slotIndex` = "0", `format`, `uid`, `name`, `vendor`, `path` = "", `nIn` = "0", `nOut` = "0", `state` = "" (base64 plugin state chunk, written only when non-empty; this is what makes remove-plugin's inverse restore the user's parameters), `isInstrument` (proposal 37 P3b; written only when TRUE, so every pre-P3b script re-serializes byte-identically — omitted means "ask the registry", which is how the built-in `uid='tw.native.303'` works with no attribute at all, while a module that was never scanned has to say so). An INSTRUMENT descriptor lands in slot 0 whatever `slotIndex` says, a SECOND instrument on the track is REFUSED, and an effect asking for slot 0 on a track that has one is clamped to slot 1 (design D3) |
| `load-project` | SLoadProjectAction | persistence/src/sloadprojectaction.cpp | `path` = "" |
| `metronome-disable` | SMetronomeAction | actions/src/smetronomeaction.cpp | (none) |
| `metronome-enable` | SMetronomeAction | actions/src/smetronomeaction.cpp | (none) |
| `metronome-toggle` | SMetronomeAction | actions/src/smetronomeaction.cpp | (none) |
| `midi-in-event` | SMidiInEventAction | testkit/src/smidiintestactions.cpp | `kind` = "noteon" (noteon\|noteoff\|cc\|pitchbend\|programchange\|channelpressure\|polypressure), `key` = "60", `velocity` = "100" (the message's LAST data byte: velocity, CC value, program), `channel` = "0" (0-BASED, like `add-note`'s), `cc` = "-1" (kind=cc; overrides `key`), `bytes` = "" ("90 3C 64" - raw hex INSTEAD of the fields, for anything the field form cannot spell), `atFrame` (optional), `timeoutMs` = "10000". Injects ONE message into the CAPTURE MIDI INPUT (`CaptureMidiInput::inject`, proposal 37 P7a), which a `--test-case` run already selects. REJECTS unless `SMARAGD_MIDI_BACKEND` is `capture`. `atFrame` is valid ONLY while the transport is PLAYING and is REJECTED when it is not - a project frame is a position on a moving playhead (design D2) - and it is resolved by WAITING for the locator to reach it, because the engine's delivered-frame clock is proposal 21 L1a's deliverable; accuracy is the locator's publication granularity, so assert on what was CAPTURED, not on the injection. NOTHING CONSUMES A MidiInput YET (L1a/L3a attach the live lane and the recorder), so today the event reaches the port and stops there |
| `midi-in-replay` | SMidiInReplayAction | testkit/src/smidiintestactions.cpp | `filePath` (a `.mid`/`.midi` read with twSmf, or a TEXT log: `# comment` lines plus `<offsetMs> <hex bytes>` per line), `track` = "-1" (SMF track index; -1 = every track merged), `channel` = "-1" (0-based override for every message; -1 = as authored), `startFrame` (optional; same PLAYING-only rule as `atFrame`), `timeoutMs` = "10000". Injects a whole performance REAL-TIME PACED on `MidiOutScheduler::hostNowNs()` - the one steady clock `CaptureBackend` stamps its blocks with, so what was sent can be mapped onto what was heard through `frameAtHostTime`. Deadlines are ABSOLUTE from the start instant, so a late message does not drag the ones behind it. A `.mid` is timed by ITS OWN ppq and tempo metas (500000 us/quarter until one says otherwise), never by the project tempo - a performance file describes a performance. Note-offs are SYNTHESISED from each note's `duration`, because no table holds one (events/CONTRACT inv. 8-9) |
| `move-automation-point` | SMoveAutomationPointAction | objects/track/src/sautomationactions.cpp | `owner` (index path from the root mixer; the TARGET's space decides how it is resolved — `self:` as a LANE, `param:` as a lane plus `slotIndex`, `cut:` as a PLACEMENT), `target` (`self:Volume` \| `self:Muted` \| `param:<id>` \| `cut:Gain` \| `cut:VelocityScale` \| `cut:Transpose`), `slotIndex` = "-1", `take` = "-1" (< 0 = the ACTIVE take of a stack), `time`, `value` (the OLD pair), `toTime`, `toValue`. Coalescing: `mergeKey` is owner + slot + target + the STARTING time, and a merge requires the newcomer to start where this one left the point, so one drag is one undo step |
| `move-clip` | SMoveClipAction | objects/track/src/smoveclipaction.cpp | `clip`, `destTrack`, `startTime` = "0", `broadcast` = "1" (edit groups; same-track moves only) |
| `move-track` | SMoveTrackAction | objects/mixer/src/smovetrackaction.cpp | `source`, `toIndex` = "-1" |
| `place-asset` | SPlaceAssetAction | objects/mixer/src/splaceassetaction.cpp | `assetName` = "", `trackPath` = "", `timePos` = "0" |
| `place-clip` | SPlaceClipAction | objects/cut/src/splaceclipaction.cpp | `trackPath`, `filePath`, `timePos` = "0", `startOffset` = "0", `duration` = "0" (0 = full wave) |
| `place-recording` | SPlaceRecordingAction | objects/cut/src/splacerecordingaction.cpp | `trackPath`, `filePath`, `timePos` = "0", `srcOffset` = "0", `length` = "0" (0 = to the end of the file). Plans takes for covered columns + plain cuts for gaps; one atomic composite. `srcOffset`/`length` place a SUB-RANGE of the recording (proposal 21 L3b), which is what makes LOOP RECORDING one call per pass with no new machinery: all passes go at the loop start, and this verb's own planner turns pass 2 onto pass 1's column as a take |
| `place-midi-recording` | SPlaceMidiRecordingAction | objects/midi/src/smidirecordactions.cpp | `trackPath`, `timePos` = "0" (FRAMES), `durationTicks` = "0" (0 = one bar), `mode` = "new-take" (new-take \| overdub \| replace), `quantize` = "off" (a `quantize-notes` grid spelling), `name` = "", plus `<e .../>` / `<n .../>` CHILDREN in the PASS WINDOW's ticks (zero = `timePos`). `place-recording` for EVENTS, and the same planner shape: an event column overlapping the pass window gets the pass - as a new TAKE, as a MERGE into the active take, or REPLACING the notes inside the window, per `mode` - while an uncovered pass becomes an `insert-midi-clip` carrying the events, born `timebase=beats` like every event placement. `quantize` appends a `quantize-notes` INSIDE this verb's own composite, so input quantise is one step of the same undo entry as the placement. The whole verb is ONE composite: its inverse undoes the placement, the merge and the quantise together (proposal 21 L4) |
| `plugin-editor-set-param` | SPluginEditorSetParamAction | testkit/src/spluginuitestactions.cpp | `trackPath` = "" (index-path; falls back to `trackIndex`), `trackIndex` = "0", `slotIndex` = "0", `paramId` = "0", `value` = "0" — drives the `SPluginParamEditor` slider a double-click opens (never shown), so the resulting `set-plugin-param` is what lands on the undo stack |
| `quantize-notes` | SQuantizeNotesAction | objects/midi/src/smidieventactions.cpp | `clip`, `grid` = "1/16" (also `1/8t` triplet, `1/4d` dotted), `strength` = "1.0" (0 = no-op, 1 = snap), `swing` = "0" (fraction of the grid, applied to every odd slot), `take` = "-1", `broadcast` = "1". A `set-notes` composition, so its inverse is simply the previous table |
| `remove-asset` | SRemoveAssetAction | objects/mixer/src/sremoveassetaction.cpp | `assetName` |
| `remove-automation-lane` | SRemoveAutomationLaneAction | objects/track/src/sautomationactions.cpp | `owner` (index path from the root mixer; the TARGET's space decides how it is resolved — `self:` as a LANE, `param:` as a lane plus `slotIndex`, `cut:` as a PLACEMENT), `target` (`self:Volume` \| `self:Muted` \| `param:<id>` \| `cut:Gain` \| `cut:VelocityScale` \| `cut:Transpose`), `slotIndex` = "-1", `take` = "-1" (< 0 = the ACTIVE take of a stack). The inverse carries the whole point list and the mode |
| `remove-automation-point` | SRemoveAutomationPointAction | objects/track/src/sautomationactions.cpp | `owner` (index path from the root mixer; the TARGET's space decides how it is resolved — `self:` as a LANE, `param:` as a lane plus `slotIndex`, `cut:` as a PLACEMENT), `target` (`self:Volume` \| `self:Muted` \| `param:<id>` \| `cut:Gain` \| `cut:VelocityScale` \| `cut:Transpose`), `slotIndex` = "-1", `take` = "-1" (< 0 = the ACTIVE take of a stack), `time`, `value` (optional — points are addressed by their OLD `(time, value)` pair, design §3.4; omitting `value` addresses by time alone, which is unambiguous because a lane holds at most one point per frame) |
| `remove-event` | SRemoveEventAction | objects/midi/src/smidieventactions.cpp | `clip`, `kind` = "" (any), `tick`, `channel` = "-1" (wildcard), `p` = "-1" (wildcard), `take` = "-1". REJECTS when nothing matched, so a typo in a script fails the case instead of testing nothing |
| `remove-from-selection` | SRemoveFromSelectionAction | selection/src/sremovefromselectionaction.cpp | `paths` = "" |
| `remove-note` | SRemoveNoteAction | objects/midi/src/smidieventactions.cpp | `clip`, `tick`, `key` = "-1" (wildcard), `channel` = "-1" (wildcard), `take` = "-1", `broadcast` = "1". REJECTS when no note matched |
| `remove-plugin` | SRemovePluginAction | objects/track/src/sremovepluginaction.cpp | `trackPath`, `slotIndex` = "0", `format`, `uid`, `name`, `vendor`, `path` = "", `nIn` = "0", `nOut` = "0", `state` = "" (captured in `apply()` from the live plugin and handed to the inverse; a hand-written element carries none) |
| `remove-sample` | SRemoveSampleAction | objects/cut/src/sremovesampleaction.cpp | `trackPath` = "0" (index-path from the root mixer, so a lane nested in a folder track is addressable; the legacy top-level-only `trackIndex` is still accepted on read), `clipIndex` = "0", `filePath` = "", `timePos` = "0" |
| `remove-take` | SRemoveTakeAction | objects/cut/src/sremovetakeaction.cpp | `clip`, `take` = "0", `thenActivate` = "-2" |
| `remove-track` | SRemoveTrackAction | objects/mixer/src/sremovetrackaction.cpp | `trackPath` = "0" (index-path from the root mixer, so a track NESTED in a folder can be removed; the legacy top-level-only `index` is still accepted on read), removes the whole subtree. The inverse pins the removed track on the action object and re-attaches it under its ORIGINAL parent — the mixer and a folder detach/re-attach differently, so the parent path is carried, not assumed |
| `render` | SRenderAction | actions/src/srenderaction.cpp | `filename` = "", `format` = "wav", `quality` = "10", `durationSec` = "-1" (< 0 = the project's duration, i.e. every pre-existing case is unchanged). The override exists because `SProject::getDurationSeconds()` is a hard-coded `return 60.0;` with a "TODO: calculate from arrangement" beside it, so EVERY render here is 60 s regardless of content — a 4 s project renders 11.5 MB, 93% silence. Proposal 36 B1a needed committed goldens that are "a gate, not a demo". Since 2026-08-15 every case in the suite but `channel_assert_dupmono` and the macOS-only `au_*` carries a DERIVED bound (`max(startFrame + frameCount)` over its own assertions, + margin) as a stopgap for this long-running branch — see `main/testkit/CONTRACT.md` inv. 10 for the derivation rule and for the cases deliberately left unbounded |
| `reorder-plugin` | SReorderPluginAction | objects/track/src/sreorderpluginaction.cpp | `trackPath`, `fromIndex` = "0", `toIndex` = "0" (both validated against the chain; the inverse is the reverse move, not a swap) |
| `reparent-track` | SReparentTrackAction | objects/mixer/src/sreparenttrackaction.cpp | `source`, `destParent`, `destIndex` = "-1" |
| `report-page-memory` | SReportPageMemoryAction | testkit/src/sreportpagememoryaction.cpp | `label` = "", `maxPages` = "-1", `maxBytes` = "-1" (< 0 = report only) — logs resident frozen-page memory: the global page count and sample bytes, the part of it held in component caches, the part held ELSEWHERE (scheduler bindings, an audio callback, a stalePredecessor chain), a per-component-TYPE breakdown, the CapturePagePool reservation beside it, and (proposal 36 B7) `captureBuffers=` — the resident twCapturingSource buffers, which are the ONE allocation in the engine that multiplies by channel width and are unrelated to the identically-named pool. There is no page pool to query — pages are `make_shared` on demand into unbounded per-component maps — so this is the only answer to "how much page memory does this project hold". The two bounds default OFF: a page count is a function of the readahead and the worker count, so a tight one would be a flake generator |
| `resize-clip` | SResizeClipAction | objects/cut/src/sresizeclipaction.cpp | `clip`, `startTime` = "0", `startOffset` = "0", `duration` = "0", `loopLength` = "0", `stretch` = "1.0", `take` = "-1" (stacks: which take the slip targets), `broadcast` = "1" (edit groups) |
| `save-project` | SSaveProjectAction | persistence/src/ssaveprojectaction.cpp | `path` = "" |
| `screenshot` | SScreenshotAction | testkit/src/sscreenshotaction.cpp | `filename` = "", `resolution` = "100%" |
| `select-take` | SSelectTakeAction | objects/cut/src/sselecttakeaction.cpp | `clip`, `take` = "-1", `broadcast` = "1" (edit groups: same take index on every member) |
| `select-track` | SSelectTrackAction | testkit/src/strackselectionactions.cpp | `trackPath` = "0", `modifiers` = "" ("ctrl" / "shift" / "ctrl+shift") — ONE real track-head click. Plain replaces the selection with this lane, Ctrl toggles its membership (and moves the range ANCHOR), Shift selects the lane range from the anchor to this lane WITHOUT moving the anchor. Not undoable: a track selection is view state |
| `set-automation-mode` | SSetAutomationModeAction | objects/track/src/sautomationactions.cpp | `owner` (index path from the root mixer; the TARGET's space decides how it is resolved — `self:` as a LANE, `param:` as a lane plus `slotIndex`, `cut:` as a PLACEMENT), `target` (`self:Volume` \| `self:Muted` \| `param:<id>` \| `cut:Gain` \| `cut:VelocityScale` \| `cut:Transpose`), `slotIndex` = "-1", `take` = "-1" (< 0 = the ACTIVE take of a stack), `mode` = "trim". **Trim** (the default, design §11 decision 3) composes the curve with the static value — for `self:Volume` that is the dB SUM; **Read/Touch/Latch/Write** make the curve the whole value; **Off** stores the lane and consumes nothing. Touch/Latch/Write are UI recorders (P6) and read exactly like Read |
| `set-automation-points` | SSetAutomationPointsAction | objects/track/src/sautomationactions.cpp | `owner` (index path from the root mixer; the TARGET's space decides how it is resolved — `self:` as a LANE, `param:` as a lane plus `slotIndex`, `cut:` as a PLACEMENT), `target` (`self:Volume` \| `self:Muted` \| `param:<id>` \| `cut:Gain` \| `cut:VelocityScale` \| `cut:Transpose`), `slotIndex` = "-1", `take` = "-1" (< 0 = the ACTIVE take of a stack), `from` = "0", `to` = "-1" (< 0 = to the end of time), child `<p t= v= c= k=/>`. THE batch verb: the absolute new state of the window [from, to), everything outside untouched; the inverse is the same verb over the same window carrying what was there. `mergeKey` = owner + target (design §3.4), and only an IDENTICAL window merges — a wider one would silently drop the points this action wrote outside the newcomer's span. `set-track-volume` / `set-track-mute` on a track whose lane is in a Read-family mode BECOME one of these, one point at the locator |
| `set-clip-name` | SSetClipNameAction | objects/cut/src/ssetclipnameaction.cpp | `clip`, `name` = "" (ABSOLUTE; the SObject SName drawn on the clip body), `take` = "-1" (stacks: which take is renamed; the name is per-take), `broadcast` = "1" (edit groups) |
| `set-edit-group` | SSetEditGroupAction | objects/track/src/seteditgroupaction.cpp | `trackPath`, `group` = "0" (0 = ungrouped) |
| `set-events` | SSetEventsAction | objects/midi/src/smidieventactions.cpp | `clip`, `take` = "-1", child `<e k= t= d= ch= key= p= v= v2= text= blob= m=/>` elements - the ABSOLUTE new table. THE universal inverse: every other content verb hands back one of these carrying the previous state, which is what makes "the inverse is the previous state" true by construction |
| `set-formant-preserve` | SSetFormantPreserveAction | objects/cut/src/ssetformantpreserveaction.cpp | `clip`, `on` = "0" (ABSOLUTE; keeps the spectral envelope fixed while the vocoder's pitch stage moves the harmonics), `take` = "-1" (stacks: which take is flagged; the flag is per-take), `broadcast` = "1" (edit groups) |
| `set-lane-view` | SSetLaneViewAction | testkit/src/slanelayouttestactions.cpp | `trackHeight` = "0" (vertical zoom, absolute), `topRow` = "-1" (vertical scroll), `laneScaleRow` = "-1" + `laneScale` (one track's own lane height), `toggleTakesRow` = "-1"; **since proposal 37 P6** `automationTrack` = "0" + `automationTarget` + `automationSlot` = "-1" + `showAutomation` (1 = show that lane as a sub-lane, 0 = hide it — what the "Show automation ▸" picker does) and `clipEnvelopes` (1 = arm `cut:Gain` editing on clip bodies; OFF by default, which is what keeps every clip-body gesture unchanged). VIEW state, not project state: not undoable and nothing here is saved. It exists so a case can put the view into the states in which the heads used to come unstuck from the lanes |
| `set-link-timebase` | SSetLinkTimebaseAction | objects/midi/src/smidiclipactions.cpp | `clip`, `timebase` = "beats" \| "time" (ABSOLUTE). Beats is the default for EVENT content and time for audio; switching preserves the clip's current position and only changes what happens to it on the next `set-tempo` |
| `set-midi-cut` | SSetMidiCutAction | objects/midi/src/smidiclipactions.cpp | `clip`, `transpose` = "0" (semitones, clamped to +/-48), `velocityScale` = "1", `channel` = "-1" (-1 = keep the recorded one), `take` = "-1", `broadcast` = "1". ABSOLUTE, like `set-pitch`. Per-CLIP modifiers: the shared sequence is never rewritten. The WINDOW (length, slip, loop, rate) is NOT here - it goes through the generalized `resize-clip`, so there is no second window verb to keep in step |
| `set-notes` | SSetNotesAction | objects/midi/src/smidieventactions.cpp | `clip`, `take` = "-1", `broadcast` = "1", child `<n tick= dur= key= velocity= channel= releaseVelocity= m=/>` elements. THE batch verb: a piano-roll drag of N notes is one undo step (it coalesces by clip + take). The children are the absolute new NOTE state; every non-note event survives, so drawing notes never destroys the CCs underneath them |
| `set-pitch` | SSetPitchAction | objects/cut/src/ssetpitchaction.cpp | `clip`, `cents` = "0" (ABSOLUTE, clamped to ±2400), `take` = "-1" (stacks: which take is transposed; pitch is per-take), `broadcast` = "1" (edit groups) |
| `set-plugin-bypass` | SSetPluginBypassAction | objects/track/src/ssetpluginbypassaction.cpp | `trackPath`, `slotIndex` = "0", `bypassed` = "false" (ABSOLUTE, not a toggle) |
| `set-plugin-param` | SSetPluginParamAction | objects/track/src/ssetpluginparamaction.cpp | `trackPath`, `slotIndex` = "0", `paramId` = "0", `value` = "0" (ABSOLUTE, clamped to the plugin's declared range; an unknown `paramId` is REJECTED; coalesces by (trackPath, slotIndex, paramId) so one slider drag is one undo entry; refused on a Missing/Unsupported slot) |
| `set-option` | SSetOptionAction | testkit/src/smidiouttestactions.cpp | `key`, `value` - one per-user `SSettings` key (the twin of `set-property`, which writes a per-PROJECT property). It exists because two of the MIDI-out behaviours D6 specifies are per-user options rather than project state: `midi/chaseNoteOns` and `midi/outOffsetMs`. NOTE it writes the developer's real `smaragd.ini`, exactly as the Options dialog would - a case that changes a value must set it back, and only ONE case in the suite may own any given key |
| `set-project-channels` | SSetProjectChannelsAction | actions/src/ssetprojectchannelsaction.cpp | `channels` = "2" (ABSOLUTE; the project's channel count, persisted as `<SProject channels='N'>`). Only 1 / 2 / 4 / 6 / 8 are permitted — anything else is REJECTED by `apply()` rather than clamped, so pair it with `expectReject="true"`. Setting the width it already has is applied but pushes no undo step. Coalesces (one constant merge key: there is one channel count per project). **Proposal 36 B4: this now REACHES THE GRAPH** — `SProject::channelsChanged` drives `STrack::setChannels` and `SStdMixer::setChannels`, so it is the width of every page the track and master path freeze, and the undo direction (a SHRINK) is ordinary because a width change creates and destroys no components. **Proposal 36 B5: it now reaches the FILE and the DEVICE too** — `RenderParams::channels` derives from the project, so a `channels='6'` project renders a 6-channel file (`qxa.mc_six_channel`) and `twSpeaker` maps the project's channels onto the device (mono fans out; otherwise positional; extra project channels dropped, extra device channels silent). It still reaches `tw303aEnvironment` not at all. B4's note here said a render across the change was byte-identical, correctly for B4: the file's channel count moves with the project now, and only the per-channel AUDIO is unchanged (`qxa.mc_width_change`) |
| `set-property` | SSetPropertyAction | actions/src/ssetpropertyaction.cpp | `key`, `value` |
| `set-selection` | SSetSelectionAction | selection/src/ssetselectionaction.cpp | `paths` = "" |
| `set-tempo` | SSetTempoAction | objects/midi/src/smidiclipactions.cpp | `bpm` = "120". THE ONLY tempo write (D2). It stores `round(6e7/bpm)` microseconds per quarter in the project's `twTempoMap` - BPM is a derived view - and then RE-DERIVES `startTime` for every `timebase=beats` link in the project, nested containers and assets included, so a MIDI clip at bar 5 stays at bar 5 while audio does not move. The ticks themselves never change, which is what stops repeated tempo edits from drifting. Coalesces (a spin-box drag is one undo step) |
| `set-track-input` | SSetTrackInputAction | objects/track/src/sliveinputactions.cpp | `trackPath`, `input` = "" . The per-track input selector (design D9), a PORTABLE string exactly the way `midiOutPort` is a portable name: `none` | `audio:<device>:<mask>` (mask HEX over the device's channels; empty or 0 = channel 0) | `midi:<port>:<ch\|any>` | `keyboard`. ABSOLUTE, and the inverse carries the previous spelling. An unknown scheme is REFUSED rather than stored and silently ignored, because a typo is otherwise indistinguishable from an input that is not plugged in. Since L2 all four spellings are live. `midi:<port>:<ch|any>` and `keyboard` attach the track's notes to the CONSUMING instrument's slot 0 - itself when it holds one, its folder when it bubbles its events up (design D4) - so an armed MIDI child of a folder drum machine is a MIDI SOURCE while the FOLDER is what leaves the frozen sum. A MIDI input whose notes would reach no instrument at all makes the track no kind of source, because excluding it would trade the arrangement for silence |
| `set-monitor-mode` | SSetMonitorModeAction | objects/track/src/sliveinputactions.cpp | `trackPath`, `mode` = "auto" \| "on" \| "off" (ABSOLUTE). AUTO is TAPE-MACHINE style (Cubase "Tapemachine", REAPER "auto"): the input sounds while the transport is STOPPED or RECORDING and gives way to the track's own material on plain Play. ON always monitors, OFF never does. Serialized only when it is not `auto` |
| `set-track-midi-routing` | SSetTrackMidiRoutingAction | objects/track/src/ssettrackmidiroutingaction.cpp | `trackPath`, `routing` = "auto" \| "parent" \| "none" (ABSOLUTE). How this track's events reach its parent (design 3.2.1): `auto` = "consumed here, or bubbled up" (pass up iff there is neither an instrument slot nor a MIDI-out port), `parent` forces bubbling even past a local consumer, `none` keeps them local |
| `set-track-midi-output` | SSetTrackMidiOutputAction | objects/track/src/ssettrackmidioutputaction.cpp | `trackPath`, `port` = "" (a PORTABLE device NAME, never a machine-local id - the id it resolves to lives in `SSettings` keyed by that name, like the audio device; "" = no MIDI output), `channel` = "-1" (0-BASED, 0..15, matching `twEvent::channel` and `add-note channel=`; -1 = "as authored"), `offsetMs` = "0" (signed, +-500, POSITIVE = send EARLIER - the outboard-gear case; added to the global default in Options -> MIDI). ABSOLUTE, and its inverse carries the whole previous state. Gaining or losing a port also changes the track's `auto` event routing ("consumed here, or bubbled up"), so the track range-invalidates on that transition |
| `set-track-mute` | SSetTrackMuteAction | objects/track/src/ssettrackmuteaction.cpp | `trackPath` (index-path from the root mixer, so a lane nested in a folder track is addressable; the legacy top-level-only `trackIndex` = "0" is still accepted on read and wins only when no path is given), `muted` = "0" (ABSOLUTE, not a toggle) **Since proposal 37 P5**: on a track whose `self:%s` lane is in a READ-family mode (read/touch/latch/write) this verb does not write the static value at all — it becomes a `set-automation-points` writing one point at the LOCATOR, because otherwise the history and the lane disagree and undo carries a step nobody can hear. Trim and Off are deliberately not redirected (design §11 decision 3) |
| `set-track-name` | SSetTrackNameAction | objects/track/src/ssettracknameaction.cpp | `trackPath` (index-path from the root mixer, so a lane nested in a folder track is addressable; the top-level-only `trackIndex` = "0" is accepted on read, for symmetry with the other track verbs), `name` = "" (ABSOLUTE — undo restores the exact previous string, because a name has no meaningful increment). The track head's name field submits it. Deliberately NOT broadcast over the selection, unlike mute/solo/arm: one typed string on four selected lanes would just give four identically-named tracks |
| `set-track-solo` | SSetTrackSoloAction | objects/track/src/ssettracksoloaction.cpp | `trackPath` (index-path from the root mixer — solo on a NESTED lane is the case this verb exists for), `solo` = "0" (ABSOLUTE, not a toggle; audibility is resolved by app/model/ssolorules.h at every summing container) |
| `set-track-volume` | SSetTrackVolumeAction | objects/track/src/ssettrackvolumeaction.cpp | `trackPath` = "0" (index-path from the root mixer, so a nested lane's fader is addressable; the legacy top-level-only `trackIndex` is still accepted on read), `volume` = "0" (dB). Coalesces consecutive drags on the same lane — `mergeKey` is the PATH, so two faders at the same index in different folders no longer merge into one undo step **Since proposal 37 P5**: on a track whose `self:%s` lane is in a READ-family mode (read/touch/latch/write) this verb does not write the static value at all — it becomes a `set-automation-points` writing one point at the LOCATOR, because otherwise the history and the lane disagree and undo carries a step nobody can hear. Trim and Off are deliberately not redirected (design §11 decision 3) |
| `slip-clip` | SSlipClipAction | testkit/src/sslipclipaction.cpp | `clip`, plus at least one of `startOffset` / `srcStart` / `loopStart` (all ABSOLUTE; `startOffset` and `loopStart` warped-domain frames, `srcStart` a source-domain exact rational). ONE tick of a live slip drag: calls the `SCut` setter directly, submits no `SResizeClipAction` and pushes no undo step, so it leaves the model in the state a drag holds between press and release. That is the only way to test the slip setters' own invalidation — `resize-clip` and a released `drag-clip-edge` both commit through `setWindow`, whose `durationChanged` reaches `twTrackMix::updateClip` and stales the extent regardless |
| `snap-to-grid-disable` | SSnapToGridAction | actions/src/ssnaptogridaction.cpp | (none) |
| `snap-to-grid-enable` | SSnapToGridAction | actions/src/ssnaptogridaction.cpp | (none) |
| `snap-to-grid-toggle` | SSnapToGridAction | actions/src/ssnaptogridaction.cpp | (none) |
| `split-clip` | SSplitClipAction | objects/cut/src/ssplitclipaction.cpp | `clip`, `splitTime` = "0", `broadcast` = "1" (edit groups) |
| `record-start` | SRecordStartAction | testkit/src/srecordtestactions.cpp | no attributes. Begins a take on every armed track (proposal 21 L3b): the transport starts through `setPlaybackRunning` if it was not running, a capture SEGMENT opens on the app's one input pump, and a growing clip appears on each armed lane. REJECTED when nothing is armed or the input will not open. Not undoable — it is transport. Since proposal 21 L4 it drives BOTH recorders: an armed track whose `trackInput` is `midi:`/`keyboard` goes to `SMidiRecorder` (no device, no growing clip - just its port's recorder ring) and every other armed track to `SAudioRecorder`, so one press records a guitar and a synth part together |
| `record-stop` | SRecordStopAction | testkit/src/srecordtestactions.cpp | no attributes. Ends the take: the files are finalised out of the capture pages, the growing clips are removed and the material is placed as ONE undo step (one `place-recording` per armed track per loop pass). REJECTED when not recording. The MIDI half commits FIRST and in its own macro (one `place-midi-recording` per armed MIDI track per pass), while the transport is still running - every host time it captured is mapped through the playhead clock's anchor, and that anchor is only valid while the RT thread is publishing (proposal 21 L4) |
| `toggle-playback` | STogglePlaybackAction | actions/src/stoggleplaybackaction.cpp | `play` = "0" |
| `toggle-selection` | SToggleSelectionAction | selection/src/stoggleselectionaction.cpp | `paths` = "" |
| `track-head-toggle` | STrackHeadToggleAction | testkit/src/strackselectionactions.cpp | `trackPath` = "0" (the lane must have a HEAD, i.e. be visible), `control` = "mute" (mute / solo / arm / takes / group), `on` = "1" (ABSOLUTE state to drive the button to; a no-op when it is already there) — presses the REAL button, which is the only route to the BROADCAST: a press on a head that is part of the selection reaches every selected lane, as ONE undo step. A press on a head OUTSIDE the selection acts on that lane alone. Not undoable itself; `<undo count="1"/>` reverses the macro the button pushed |
| `virtual-key` | SVirtualKeyAction | testkit/src/seventuitestactions.cpp | `key` = "60" (MIDI note), `velocity` = "100", `durationTicks` = "960", `hold` = "0", `release` = "0", `durationMs` = "0". TWO MODES on the REAL `SVirtualKeyboardDock`, because the widget does two things. **Default** — WRITE: insert a note at the LOCATOR through `add-note`. The target is the first event clip in the SELECTION, else the first one on the SELECTED track; with neither the verb FAILS (assert that with `expectReject`). The nested `add-note` is what lands on the undo stack, so `<undo count="1"/>` removes the note. **`hold="1"`** — PLAY: a note-on on the computer keyboard's in-process MIDI PORT (`KeyboardMidiInput`, proposal 21 L2 / design D9), which a live-armed instrument track hears exactly as it hears hardware; nothing is written and nothing lands on the undo stack. **`release="1"`** sends the matching note-off, and `key="-1"` releases every key the dock is holding. **`durationMs="N"`** with `hold` sends the note-off N ms later, paced on `MidiOutScheduler::hostNowNs()` and PUMPING the event loop while it waits |
| `wait-ms` | SWaitMsAction | testkit/src/smidiouttestactions.cpp | `ms` = "0" - pump the event loop for a wall-clock duration. `wait-playhead` covers "playback got somewhere" and is the right verb almost everywhere; it cannot express "let the transport run for N seconds" under a CYCLE, where the locator wraps and neither the relative nor the absolute form has a target that means "two passes". Pumping the event loop is the POINT: every main-thread timer the behaviour rides on - the locator poll, the meters, the MIDI-out pump - only runs while events are processed |
